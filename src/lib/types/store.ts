export interface StoreData {
  name: string
  description: string
  shortDescription: string
  address: string
  type: string
  commitments: string[]
}

export interface StoreImage {
  src: string
  alt: string
}